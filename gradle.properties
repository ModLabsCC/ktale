kotlin.code.style=official
org.gradle.configuration-cache=true

# Required by build.gradle.kts (mirrors your plugin build convention).
# You can override with:  ./gradlew -Dkotlin_version=...
systemProp.kotlin_version=2.2.21

# Kotlin may not support JVM target 25 yet, even if you compile with JDK 25.
# Set to 25 when your Kotlin version supports it; otherwise keep at 21.
systemProp.ktale_jvm_target=21